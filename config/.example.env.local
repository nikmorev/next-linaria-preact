STARTUP_COMMAND='npm run dev' # start development server
# STARTUP_COMMAND='tail -f /dev/null' # uncomment to run container in idle mode so you can manually control (start/stop) dev server inside container
MAIN_API_URL="http://example.local" # Replace XX with number of your dev server (where main backend is running)